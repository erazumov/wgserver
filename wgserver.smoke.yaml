http:
  addr: "127.0.0.1:18080"
  health_addr: "127.0.0.1:19090"

db:
  path: "/tmp/wgserver-smoke.sqlite"

clients:
  interface: "wg0"
  listen_port: 51820
  address: "10.0.1.1/24"
  cidr: "10.0.1.0/24"
  dns_servers:
    - "1.1.1.1"
  endpoint: "127.0.0.1:51820"
  public_key: "WG0_PUB_BASE64"

telegram:
  bot_token: "REPLACE_ME"
  group_chat_id: 0
  per_user_quota: 2

update:
  enabled: false
  github_repo: "erazumov/wgserver"
  check_interval_minutes: 60
